# 비레스 시작상황별 기록 공개 사이트

`05_뷰어/review.html`의 공개판을 Sites에 배포하기 위한 vinext 프로젝트다.

## 갱신

1. 상위 폴더에서 `node 90_도구/build_viewer.mjs`를 실행한다.
2. 이 폴더에서 `npm run sync:content`를 실행한다.
3. `npm run build`로 공개판을 검증한다.

공개 데이터에는 시작상황 7종과 초기 열람 원문 14편만 포함한다.
